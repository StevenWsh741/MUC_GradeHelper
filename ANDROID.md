# 安卓远程提醒

`MUC成绩远程提醒.apk` 是桌面查分器的安卓伴侣应用。手机不直接访问学校系统，也不接触浏览器 Cookie；可选择在手机 Keystore 加密保险箱中保存学号和密码，用于远程启动电脑端网页查分。

## 安装与配对

1. 将 `MUC成绩远程提醒.apk` 传到安卓手机并安装。安装时需允许当前文件管理器“安装未知应用”。
2. 在电脑上双击 `配置手机提醒.cmd`，程序会生成随机私密频道和 256 位加密密钥。
3. 将 `mucgrade-v1:` 开头的配对码复制到手机。
4. 打开 App，粘贴配对码，点击“开启远程提醒”并允许通知。
5. 在电脑上双击 `测试手机提醒.cmd`。测试只发送随机虚构课程和分数。

## 用手机远程启动电脑查分

1. 电脑必须开机、联网且不处于休眠状态。
2. 电脑双击 `启动手机远程控制.cmd`，看到“正在等待手机远程启动指令”后保持窗口开启。
3. 在 App 中输入学号和统一身份认证密码，点击“加密保存并让电脑开始查分”。
4. 手机会先收到“电脑正在打开网页”回执，成功登录并读取成绩表后再收到“正在自动查分”回执；有新成绩时仍会加密推送回手机。
5. 后续可以不再输入学号和密码，直接点击启动按钮。点击“清除本机保存的学号和密码”可立即删除保险箱内容。

如需每次登录 Windows 后自动开始等待，双击 `设为电脑开机自动等待手机.cmd`。该功能只在当前 Windows 用户的启动文件夹建立快捷方式，不需要管理员权限；可用 `关闭电脑开机自动等待.cmd` 取消。

电脑端只接受两分钟内的新指令，并拒绝重放或重复启动。如果电脑监听器断网，该次启动指令不会被服务器缓存，请在电脑重连后再点一次。

安卓系统会显示一条持续的“远程提醒已开启”通知，这是保持跨网络实时连接所必需的前台服务。部分手机还需在系统设置中允许 App 自启动，并将电池策略设为“不限制”。

## 隐私与传输

- 电脑端使用 AES-256-GCM 加密后再发送，中转服务只能看到密文。
- 每个配对都使用随机频道、随机密钥、随机 IV 和消息随机值。
- 为防止手机短时断网漏报，中转端最多缓存密文 24 小时；App 仅保存不含成绩的断线续传游标。
- App 不保存成绩历史。配对密钥由 Android Keystore 生成的设备密钥加密后，才写入 App 私有存储。
- 可选的学号与密码使用独立 Android Keystore AES-GCM 密钥加密保存；App 禁止备份，并提供独立清除按钮。
- 手机启动指令采用 AES-256-GCM 端到端加密、不进入中转缓存，电脑解密后只将凭据放入新启动的查分进程环境，不写入文件或日志。
- 电脑端 `remote-config.local.json` 只含随机频道与密钥，不含成绩，并已加入 `.gitignore`。

默认使用 `https://ntfy.sh` 作为密文中转。代码也支持将配对配置的 `endpoint` 替换为自建 ntfy 服务。

## 从源码构建

要求 Android SDK 35、JDK 17 或更高版本。在 `android` 目录运行：

```powershell
.\build-apk.ps1
```

安装包会输出到项目根目录的 `MUC成绩远程提醒.apk`。
首次构建会在 `android/.signing/` 生成本机 Release 签名密钥。该目录已被 Git 忽略；请私下备份，丢失后将无法用新 APK 覆盖升级旧安装。
