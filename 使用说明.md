# MUC 本学期成绩自动检查助手

## 使用方法

1. 双击 `启动查分.cmd`。
2. 输入学号和统一身份认证密码。密码输入时不会显示，也不会保存到硬盘。
3. 程序会打开 Edge 或 Chrome，尝试登录信息门户并依次进入“本科生教务系统 → 综合查询 → 本学期成绩”。
4. 如果网页菜单未被自动识别，请在打开的浏览器里手动进入“本学期成绩”页面；程序看到成绩表后会继续工作。
5. 第一次成功读到成绩表时，会在内存中建立本次运行基线，不写入磁盘；关闭程序后基线自动消失。
6. 只有课程从“成绩为空/未公布”变为已有成绩时，才显示并播报本次新出的课程名和分数；不播报总数或旧成绩。
7. 新成绩出现时，会显示全屏大字视频提醒，并快速播报课程名和分数；按 `Esc` 或底部按钮关闭。
8. 正式启动并首次进入账号后，会读取并播报当前所有已有成绩，同时在内存中建立基线；之后只播报新成绩。
9. 保持黑色命令窗口和浏览器窗口打开。按 `Ctrl+C` 可停止。
10. 可随时双击 `测试凤凰提醒.cmd`，用随机虚构课程和分数测试视频、闪耀文字和中文语音。

## 可调整设置

用记事本打开 `config.json`：

- `checkIntervalSeconds`：基础检查间隔，默认 60 秒。
- `checkJitterSeconds`：随机浮动秒数，默认 10 秒；因此每次随机等待 50–70 秒。
- `manualNavigationTimeoutMinutes`：等待手动进入成绩页的最长时间，默认 10 分钟。
- `loginTimeoutMinutes`：等待统一认证完成的最长时间，默认 5 分钟。
- `alertIncludeScores`：提醒中是否显示课程名和成绩。
- `voiceRepeatCount`：新成绩语音播报次数，默认 2 次，可设为 1–5。
- `speechRate`：中文播报语速，默认 3，可设为 -10 至 10。
- `announceExistingScoresOnLogin`：正式登录后是否播报当前所有已有成绩，默认开启。
- `phoenixVideo`：凤凰提醒视频文件名，默认 `凤凰视频.mp4`。

## 数据与隐私

- 登录状态保存在本文件夹的 `.browser-profile` 中，仅供这个查分助手使用，已加入 `.gitignore`。
- 成绩基线只存在于当前 Node.js 进程内存，不创建成绩数据文件。
- 密码和成绩不会写入配置、提醒文本或状态文件。
- `.browser-profile`、运行日志和依赖目录均不会被未来的 Git 提交收录。

## 常见问题

- 若学校要求验证码、短信验证或确认身份，请直接在打开的浏览器中完成，程序会继续等待。
- 若学校把认证回跳错误地改成 `/qser/simpleSSOLogin`，程序会自动改用 HTTPS 认证回调重试；请不要手动停留在该错误页。
- 若登录状态过期，程序会尝试重新登录；仍需人工验证时请在浏览器内完成。
- 若提示找不到 Edge/Chrome，请先安装任意一个浏览器。
- 请勿同时启动两个查分窗口，否则浏览器登录数据目录会被占用。
