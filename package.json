{
  "name": "muc-score-checker",
  "version": "1.0.0",
  "private": true,
  "description": "中央民族大学本科生成绩自动检查与提醒",
  "main": "muc-score-checker.js",
  "scripts": {
    "start": "node muc-score-checker.js --loop",
    "check": "node --check muc-score-checker.js && node --check muc-remote-notify.js && node --check muc-remote-listener.js",
    "remote:self-test": "node muc-remote-notify.js --self-test",
    "remote:listener-test": "node muc-remote-listener.js --self-test"
  },
  "dependencies": {
    "playwright-core": "^1.54.1"
  }
}
